On line 6 inside the function code block fetch() function is executed after passing it a single string argument

```js
const fetchData = () => {
    fetch("https://randomuser.me/api/?results=1")
  };
```